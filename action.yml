name: 'Gitflow version'
description: 'Generate next version based on gitflow style.'
outputs:
  inc:
    description: "Increment"
    value: ${{ steps.incpreid.outputs.inc }}
  preid:
    description: "Pre ID"
    value: ${{ steps.incpreid.outputs.preid }}
runs:
  using: "composite"
  steps:    
    - name: Install Node16
      uses: actions/setup-node@v3
      with:
        node-version: 16

    - name: Install Semver
      run: npm install -g semver
      shell: bash

    - name: Get inc and preid
      id: incpreid
      run: |
        ls -la
        chmod +x ./get-inc-and-preid.sh
        ./get-inc-and-preid.sh
      shell: bash
